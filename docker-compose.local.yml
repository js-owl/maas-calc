# Local dev: STL calculator with hot reload on maas-shared.
# Environment: LOCAL MACHINE ONLY.
# Usage: docker compose -f docker-compose.local.yml up --build

services:
  backend-calc:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: backend-calc
    ports:
      - "5679:5679"
    environment:
      - CORS_ORIGINS=["*"]
      - CORS_ALLOW_CREDENTIALS=true
      - CORS_ALLOW_METHODS=["*"]
      - CORS_ALLOW_HEADERS=["*"]
      - PYTHONPATH=/app
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    restart: unless-stopped
    # command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7000", "--reload"]
    command: ["python", "-m", "debugpy", "--listen", "0.0.0.0:5679", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7000", "--reload"]
    networks:
      - maas-shared

networks:
  maas-shared:
    external: true

volumes:
  uploads:
  logs:
